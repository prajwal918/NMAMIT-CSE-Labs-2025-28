DATA SEGMENT
DAY DB ?
MONTH DB ?
YEAR DW ?
DATA ENDS
CODE SEGMENT
ASSUME CS:CODE, DS:DATA
START: MOV AX, DATA
       MOV DS, AX
       MOV AH, 2AH
       INT 21H
       MOV DAY, DL
       MOV MONTH, DH
       MOV YEAR, CX
       MOV AH, 4CH
       INT 21H
CODE ENDS
END START
